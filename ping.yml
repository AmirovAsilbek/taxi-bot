name: Keep Bot Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request
        run: |
          curl -s https://taxi-bot-6n72.onrender.com > /dev/null
